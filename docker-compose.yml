services:
  lgs-solver:
    build: .
    image: lgs-solver
    container_name: lgs-solver
    ports:
      - "1913:80"        # Host-Port 1913 -> hier anpassen, falls belegt
    restart: unless-stopped
